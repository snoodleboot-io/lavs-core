# Base URL of the LAVS backend. In dev the Vite proxy maps /api -> this origin
# so the HttpOnly session cookie stays same-origin. One build runs against any backend.
VITE_LAVS_API_URL=http://127.0.0.1:8001
